// two query selectors
// querySelector
// qu
console.log("tets");
// querySelector can take id, class or tag name as a parameter: returns a first occurence of that id, class or tag
// document.querySelector()
// querySelectorAll can take id, class or tag name as a parameter: returns all possible occurences of that id, class or tag
// document.querySelectorAll();
